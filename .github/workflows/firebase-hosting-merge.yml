name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🔍 Listar archivos (para depurar)
        run: ls -R

      - run: npm ci && npm run build
        working-directory: Web/invbuenosaires # <-- Dejamos esta línea, aunque sabemos que fallará
      
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_INVERSIONESBUENOSAIRES_FD68 }}
          channelId: live
          projectId: inversionesbuenosaires-fd68